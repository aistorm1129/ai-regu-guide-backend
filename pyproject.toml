[tool.poetry]
name = "ai-compliance-backend"
version = "1.0.0"
description = "AI Compliance Guide API"

[tool.poetry.dependencies]
python = "^3.11"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"